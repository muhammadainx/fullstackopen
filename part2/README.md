# Part 2: Communicating with the Server

This section focuses on integrating React applications with external data sources. It covers making HTTP requests to retrieve and update data from a backend server, handling asynchronous operations, and managing side effects using the `useEffect` hook. It also introduces separating API communication into dedicated service modules, working with forms, and managing collections of data stored on a server.
